This file simulates how the engine works

the actual logic is the same - pings the local host IP (port 3000) with the data

it just uses randomly generated data 

to get this to work with the client - first make sure the client is in game mode then run this file 